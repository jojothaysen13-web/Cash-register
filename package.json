{
  "name": "cash-register",
  "version": "0.1.0",
  "private": true,
  "description": "POS-Kassensystem (Frontend + Backend) – Root-Orchestrierung fuer Deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && node dist/db/seed.js && node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
